#include <stdio.h>
#include <stdlib.h>
#define MAX_DIST 10
int main()
{
	
	float meters = 1000.000;
	printf("Your number of quilometers converted to meters is %f\n", (meters *MAX_DIST));

	system("pause");
	return 0;
}